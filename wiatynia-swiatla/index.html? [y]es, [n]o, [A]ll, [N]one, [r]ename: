
body {
  margin: 0;
  background: linear-gradient(135deg, #000018, #1a0033);
  font-family: 'Segoe UI', sans-serif;
  color: #f8f8f8;
  overflow-x: hidden;
}
.centered {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  text-align: center;
}
.sigil {
  width: 120px;
  height: 120px;
  background: url('sigillum_lux_aeternum.png') no-repeat center/contain;
  animation: pulse 4s infinite ease-in-out;
  margin-bottom: 20px;
}
@keyframes pulse {
  0% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.05); opacity: 0.85; }
  100% { transform: scale(1); opacity: 1; }
}
.title {
  font-size: 2.5em;
  margin-bottom: 0.5em;
}
.subtitle {
  font-size: 1.2em;
  margin-bottom: 2em;
  max-width: 600px;
}
.mapa-swiadomosci {
  width: 100%;
  height: 400px;
  background: url('mapa_swiadomosci.png') no-repeat center/cover;
  border-top: 1px solid #aaa;
  border-bottom: 1px solid #aaa;
  margin-top: 30px;
}
.karty-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  max-width: 800px;
}
.karta {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid gold;
  border-radius: 12px;
  padding: 20px;
  width: 220px;
  text-align: center;
  cursor: pointer;
  transition: transform 0.3s, box-shadow 0.3s;
}
.karta:hover {
  transform: scale(1.05);
  box-shadow: 0 0 15px gold;
}
.modlitwa-output {
  margin-top: 40px;
  font-style: italic;
  opacity: 0.9;
}
